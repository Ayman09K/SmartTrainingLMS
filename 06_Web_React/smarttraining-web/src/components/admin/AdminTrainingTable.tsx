import type{TrainingResponse}from"../../types/training";import{displayLabel}from"../../utils/displayLabels";import"./Admin.css";export function AdminTrainingTable({trainings}:{trainings:TrainingResponse[]}){if(!trainings.length)return <div className="admin-panel"><h2 className="admin-panel-title">Aucune formation</h2></div>;return <div className="admin-table-wrapper"><table className="admin-table"><thead><tr><th>ID</th><th>Titre</th><th>Niveau</th><th>Durée</th><th>Statut</th></tr></thead><tbody>{trainings.map(t=><tr key={t.id}><td>#{t.id}</td><td>{t.title}</td><td>{t.level||"Non indiqué"}</td><td>{t.estimatedDurationHours??t.durationHours?`${t.estimatedDurationHours??t.durationHours} h`:"Non indiquée"}</td><td>{displayLabel(t.status||(t.active===false?"INACTIVE":"ACTIVE"))}</td></tr>)}</tbody></table></div>}
