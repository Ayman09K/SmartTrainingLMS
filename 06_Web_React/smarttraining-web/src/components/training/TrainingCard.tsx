import{Link}from"react-router-dom";import{ArrowRight,Clock,Layers}from"lucide-react";import type{TrainingResponse}from"../../types/training";import{displayLabel}from"../../utils/displayLabels";import"./TrainingCard.css";export function TrainingCard({training}:{training:TrainingResponse}){const duration=training.estimatedDurationHours??training.durationHours;const status=training.status||(training.active===false?"INACTIVE":"ACTIVE");return <article className="training-card"><div className="training-card-header"><span className="training-badge">{training.level||"Formation"}</span><span className={`training-status ${training.active===false?"inactive":"active"}`}>{displayLabel(status)}</span></div><h2>{training.title}</h2><p>{training.description||"Découvrez le contenu et les objectifs de cette formation."}</p><div className="training-meta"><span><Clock size={16}/>{duration?`${duration} h`:"Durée non indiquée"}</span><span><Layers size={16}/>Modules et leçons</span></div><Link className="training-link" to={`/learner/trainings/${training.id}`}>Continuer<ArrowRight size={17}/></Link></article>}
