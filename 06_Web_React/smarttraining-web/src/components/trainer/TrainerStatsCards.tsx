import type{AlertResponse,InterventionResponse}from"../../types/trainer";import"./Trainer.css";export function TrainerStatsCards({alerts,interventions}:{alerts:AlertResponse[];interventions:InterventionResponse[]}){const high=alerts.filter(a=>a.severity==="HIGH").length;const planned=interventions.filter(i=>i.status==="PLANNED").length;const learners=new Set(alerts.map(a=>a.learnerId)).size;return <div className="trainer-grid">{[["Alertes ouvertes",alerts.length],["Alertes fortes",high],["Apprenants à suivre",learners],["Interventions prévues",planned]].map(([l,v])=><div className="trainer-stat-card" key={l}><p className="trainer-stat-label">{l}</p><p className="trainer-stat-value">{v}</p></div>)}</div>}
